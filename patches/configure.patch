diff --git a/configure b/configure
index 68cce49..c8d0305 100755
--- a/configure
+++ b/configure
@@ -9183,7 +9183,7 @@ $as_echo "$as_me: WARNING:
 # FiXME: Use GNUPG_CHECK_LIBUSB and modify to use separate AC_SUBSTs.
 if test "$use_ccid_driver" = auto || test "$use_ccid_driver" = yes; then
    case "${host}" in
-     *-mingw32*)
+     *-mingw32* | *-*-openedition)
        LIBUSB_NAME=
        LIBUSB_LIBS=
        LIBUSB_CPPFLAGS=
@@ -15119,11 +15119,11 @@ cat confdefs.h - <<_ACEOF >conftest.$ac_ext
 #ifdef __cplusplus
 extern "C"
 #endif
-char deflateInit2_ ();
+char deflate();
 int
 main ()
 {
-return deflateInit2_ ();
+return deflate();
   ;
   return 0;
 }
