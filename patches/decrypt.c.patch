diff --git a/g10/decrypt.c b/g10/decrypt.c
index fb606cb..f4d7147 100644
--- a/g10/decrypt.c
+++ b/g10/decrypt.c
@@ -62,7 +62,9 @@ decrypt_message (ctrl_t ctrl, const char *filename, strlist_t remusr)
 
   /* Open the message file.  */
   fp = iobuf_open (filename);
-  if (fp && is_secured_file (iobuf_get_fd (fp)))
+  int fd =iobuf_get_fd (fp);
+  __disableautocvt(fd);
+  if (fp && is_secured_file (fd))
     {
       iobuf_close (fp);
       fp = NULL;
